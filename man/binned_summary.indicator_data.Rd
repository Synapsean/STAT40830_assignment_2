% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mypackage.R
\name{binned_summary.indicator_data}
\alias{binned_summary.indicator_data}
\title{Summary of unique 'indicator_name' from object 'indicator_data'}
\usage{
binned_summary.indicator_data(
  object,
  bin_size = 10,
  indicator_name = NULL,
  ...
)
}
\arguments{
\item{object}{An object of class \code{indicator_data}.}

\item{bin_size}{Numeric, the size of the year bins (e.g., 5 for 5-year bins). Defaults to 5.}

\item{indicator_name}{A character string specifying the exact name of a single indicator
to summarise. If \code{NULL} (default), the summary will be performed across all indicators
in the dataset.}

\item{...}{Not used.}
}
\value{
A \code{data.table} with binned and summarised data. The output will
include \code{country_name}, \code{year_bin_start}, \code{mean_value_bin}, \code{n_observations},
and \code{year_bin_label}. If \code{indicator_name} was provided, an \code{indicator_name}
column will also be present in the output.
}
\description{
Summarises indicator data into custom size bins,
calculating the mean value per bin for each country. Uses \code{data.table} for efficient processing.
}
